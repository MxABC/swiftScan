import XCTest
@testable import swiftScan_1

final class swiftScan_1Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swiftScan_1().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
